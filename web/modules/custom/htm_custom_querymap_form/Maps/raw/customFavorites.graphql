query ($language: LanguageId!, $id: Int!) {
  CustomFavorites(language: $language, id: $id) {
    ... on FavoriteEntity {
      favoritesNew {
        targetId
        entity {
          entityAccess(operation:"VIEW")
          entityLabel
          entityUrl {
            path
          }
        }
      }
    }
  }
}
