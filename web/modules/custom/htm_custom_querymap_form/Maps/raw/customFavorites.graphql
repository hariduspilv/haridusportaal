query ($language: LanguageId!) {
  CustomFavorites(language: $language) {
    ... on FavoriteEntity {
      favoritesNew {
        targetId
        entity {
          entityAccess(operation:"VIEW")
          entityLabel
          entityUrl {
            path
          }
        }
      }
    }
  }
}
