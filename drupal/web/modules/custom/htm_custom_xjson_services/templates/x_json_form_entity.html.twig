{#
/**
 * @file x_json_form_entity.html.twig
 * Default theme implementation to present X json form entity data.
 *
 * This template is used when viewing X json form entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_x_json_form_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('x_json_form_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
