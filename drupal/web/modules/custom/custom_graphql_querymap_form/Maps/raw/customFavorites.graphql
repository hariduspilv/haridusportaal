query ($language: LanguageId!){
  CustomFavorites(language:$language){
    ... on FavoriteEntity{
      favoritesNew{
        targetId
        entity{
          entityLabel
          entityUrl{
            path
          }
        }
      }
    }
  }
}
