query (
  $lang: String,
  $offset: Int,
  $limit: Int,
  $search_term: String!) {
  CustomElasticQuery(offset: $offset, limit: $limit, elasticsearch_index: "elasticsearch_index_drupaldb_schools",
    filter: {conjunction: AND,
      conditions: [
        {operator: EQUAL, field: "langcode", value: [$lang]}]},
  	score: {search_value: $search_term, conditions:[
      {field:"title", weight:5},
      {field:"field_registration_code", weight:5}
    ]}) {
    Nid
    Title
    EntityPath
  }
}
