query (
  $lang: String,
  $offset: Int,
  $limit: Int,
  $search_term: String!) {
  CustomElasticQuery(offset: $offset, limit: $limit, elasticsearch_index: "elasticsearch_index_drupaldb_study_programmes",
    filter: {conjunction: AND,
      conditions: [
        {operator: EQUAL, field: "langcode", value: [$lang]}]},
  	score: {search_value: $search_term, conditions:[
      {field:"title", weight:5}
    ]}) {
    Nid
    Title
    EntityPath
  }
}
